noinst_LIBRARIES = libutils.a

EXTRA_DIST = utils.dsp

libutils_a_SOURCES = getopt.c getopt1.c

noinst_HEADERS = getopt.h

# Remove these files here... they are created locally
DISTCLEANFILES = *~

@NO_WARNINGS@
