You can use (ttt) to play TicTacToe,it's easy

You can use (go) to play gnugo1.2.Gnugo1.2 played too easy,so there is no fun anymore...(but it's code is easy to port).

There is six edition on gnugo now:https://www.gnu.org/software/gnugo/download.html

It seems start with gnugo2.6,it can play well(campare with me...I played not well too...)and the structure of project start stay the same.I try to port gnugo2.6,but meet many problem and failed:

1. If I try to use gnugo2.6's makefile to make,then `ld` the `.o` files with circle's `Rlues.mk`,I have to edit `config.h` generated by `configure`.and replace `CC = gcc` to `CC = aarch64-none-elf-gcc`....and many many many many changes...but faild

2. If I try to use this project's MAKEFILE (which is also the `circle-stdlib`'s sample's MAKEFILE),there is also many place to change,and meat many errors...

so port gnugo2.6 is failed,maybe it's easy for experts.I remain the gnugo2.6's code here,may be some day I can port this

but use c's go program is not a good idea with this project:this project means it support only scheme,and app should all write in scheme.It should not as a 'c program's wapper'

Play go on console use `X` and `O` as black and white pieces can accept.but use letters as chess pieces is too ugly.so I don't want port chess's console program unless I can display chess pieces in graphic use unicode 